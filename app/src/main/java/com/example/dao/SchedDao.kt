package com.example.dao

import androidx.room.Dao

@Dao
interface SchedDao {
//    @Query("SELECT * FROM Schedule WHERE specId LIKE :curSpec AND date LIKE :curDate ORDER BY time")
//    fun getAllById(curSpec: Int, curDate: String): LiveData<List<Schedule>>

//    @Insert
//    fun insertAll(vararg users: Schedule)

//    @Delete
//    fun delete(user: Schedule)
}